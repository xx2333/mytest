#默认使用配置
spring:
  profiles:
    active: dev

---

#开发配置
spring:
  profiles: dev

  datasource:
    url: jdbc:mysql://localhost:3306/db2
    username: root
    password: zwh12345
    driver-class-name: com.mysql.jdbc.Driver